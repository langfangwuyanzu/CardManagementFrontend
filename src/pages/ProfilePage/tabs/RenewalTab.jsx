import { Paper, Typography, Box, Divider, Button } from "@mui/material";

export default function RenewalTab({ profile }) {
  

  return (
    <Paper className="card" variant="outlined">
      <div>waitng developing </div>
    </Paper>
  );
}
